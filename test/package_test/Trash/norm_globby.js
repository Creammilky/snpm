let serialize = require('serialize-javascript');
let reg = require('path-to-regexp');
let TokenData = reg.TokenData;
console.log(serialize(TokenData));
//console.log(Function.prototype.isClass.call(TokenData));
ob = new serialize;
console.log(ob);
class T {
    constructor(tokens, delimiter) {
    }
    sfjslgj()
    adads=re0
}
console.log(serialize(T).slice(0, -2));

new T